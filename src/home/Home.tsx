import { Box, Typography } from "@mui/material";

interface HomeProps {}

const Home: React.FC<HomeProps> = (props) => {

    return (
        <Box>
            <Typography>
                Salut
            </Typography>
        </Box>
    );
}

export default Home;