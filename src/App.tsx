import { Box } from "@mui/material";
import { ThemeProvider } from "@mui/material/styles";

export function App() {
  return (
    <ThemeProvider theme={}>
      <Box>
        YO
      </Box>
    </ThemeProvider>
  )
}